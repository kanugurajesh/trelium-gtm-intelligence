# Copy to .env (or export directly) before running collection/extraction.
# Not required to run the test suite or to re-render already-generated
# briefs from committed cache/evidence — those work with no key at all.

OPENAI_API_KEY=sk-...

# Optional. Defaults to gpt-4o-mini if unset.
OPENAI_MODEL=gpt-4o-mini
